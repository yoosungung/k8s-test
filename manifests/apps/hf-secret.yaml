apiVersion: v1
kind: Secret
metadata:
  name: hf-token-secret
  namespace: llm-serving
type: Opaque
data:
  # Base64 encoded Hugging Face token.
  # Generate using: echo -n "your_hf_token_here" | base64
  token: YOUR_HF_TOKEN_BASE64_HERE
